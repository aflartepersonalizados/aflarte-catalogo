const API='https://ldzvozipodwkyeuekaqw.supabase.co/functions/v1/seller-catalog-public';let all=[];const money=n=>Number(n).toLocaleString('pt-BR',{style:'currency',currency:'BRL'});const safe=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));function render(){const q=document.getElementById('q').value.trim().toLowerCase(),rows=all.filter(p=>(p.name+' '+p.sku).toLowerCase().includes(q));document.getElementById('count').textContent=rows.length+' produtos';document.getElementById('grid').innerHTML=rows.length?rows.map(p=>'<article class="card"><div class="photo">'+(p.image?'<img loading="lazy" src="'+safe(p.image)+'" alt="">':'<span>Foto em atualização</span>')+'</div><div class="sku">'+safe(p.sku)+'</div><div class="name">'+safe(p.name)+'</div><div class="price">'+money(p.price)+'</div></article>').join(''):'<div class="empty">Nenhum produto encontrado.</div>'}async function load(){try{const r=await fetch(API,{cache:'no-store'});if(!r.ok)throw 0;const d=await r.json();all=Array.isArray(d.products)?d.products:[];document.getElementById('status').textContent='Preços sincronizados com o Cadastro Mestre';render()}catch{document.getElementById('status').textContent='Não foi possível atualizar agora. Tente novamente.'}}document.getElementById('q').addEventListener('input',render);load();