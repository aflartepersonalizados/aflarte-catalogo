(()=>{const ENDPOINT='https://ldzvozipodwkyeuekaqw.supabase.co/functions/v1/catalog-request';function val(id){return document.getElementById(id)?.value?.trim()||''}function checked(name){return document.querySelector(`input[name="${name}"]:checked`)?.value||''}function orderNumber(){const s=document.querySelector('.order-number-badge strong')?.textContent?.trim()||'';return s}function getItems(){try{if(typeof cart==='undefined'||typeof products==='undefined'||!Array.isArray(cart))return[];return cart.map(i=>{const p=products.find(x=>x.id===i.id);if(!p)return null;const v=i.variantId&&Array.isArray(p.variants)?p.variants.find(x=>x.id===i.variantId):null;return{id:p.id,name:p.name,variant:v?.label||'',price:v?.price||p.price||'',qty:i.qty||1}}).filter(Boolean)}catch{return[]}}function sendRecord(){const payload={order_number:orderNumber(),customer_name:val('customerName'),customer_phone:val('customerPhone'),desired_date:val('customerDate')||null,delivery_type:checked('deliveryType')||'Retirada',delivery_address:val('customerAddress'),payment_type:checked('paymentType')||'Pix',notes:val('customerNotes'),items:getItems()};if(!payload.order_number||!payload.customer_name||!payload.items.length)return;fetch(ENDPOINT,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload),keepalive:true}).catch(()=>{});}function init(){const btn=document.getElementById('sendCart');if(!btn)return;btn.addEventListener('click',()=>setTimeout(sendRecord,0),true)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init()})();